import type { SafetyLabel } from '@/types';

export function computeSafety(studentRank: number, closingRank: number): SafetyLabel {
  // TODO: implement safety computation logic
  return 'moderate';
}
