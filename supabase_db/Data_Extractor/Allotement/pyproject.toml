[project]
name = "tnea-allotment-parser"
version = "0.1.0"
description = "Parse TNEA allotment PDFs into standardized CSV outputs."
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.12.0",
    "numpy>=2.0.0",
    "pandas>=2.2.0",
    "requests>=2.32.0",
]
